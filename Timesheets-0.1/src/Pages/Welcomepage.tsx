import { Typography } from "@mui/material";

const Welcomepage = () =>{
    return(
    <Typography variant="h3" color="error" fontWeight={600} sx={{ mt:25,textAlign:"center", flexGrow: 1, fontWeight: 600, }}>
        Welcome To Employee Time Sheets
    </Typography>
    )
}

export default Welcomepage;